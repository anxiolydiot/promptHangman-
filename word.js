var Letter= require("./letter.js'";
var Word = function(wrd){
  this.word= wrd;
  this.lets= [];
  this.found= false;
  this.getLets = function(){
    for (i=0, i <= wrd.length, i++) {
      this.lets[i] = new Object()}

  }
}